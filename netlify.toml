[build]
  publish = "."
  command = ""

[functions]
  directory = "netlify/functions"

[build.environment]
  PYTHON_VERSION = "3.8"

# 重定向規則
[[redirects]]
  from = "/api/*"
  to = "/.netlify/functions/:splat"
  status = 200